# Slack Configuration (Required)
# Bot token from OAuth & Permissions page (starts with xoxb-)
SLACK_BOT_TOKEN=xoxb-your-bot-token

# App-level token for Socket Mode (starts with xapp-)
# Generate from Basic Information > App-Level Tokens
SLACK_APP_TOKEN=xapp-your-app-token

# Langflow Configuration (Optional - for initial/default flow setup)
# If provided, creates a default flow on startup
# Additional flows can be added via Slack commands: @bot flows add ...
LANGFLOW_API_URL=https://dev-langbuilder.cloudgeometry.com
# LANGFLOW_FLOW_ID=54f54634-e187-4d42-9c7f-30c50b0f3d90
# LANGFLOW_API_KEY=sk-your-langflow-api-key
# DEFAULT_FLOW_NAME=default

# Admin Configuration (Optional)
# Comma-separated list of Slack user IDs who can manage flows
# If empty, all users can manage flows (not recommended for production)
# Get user IDs from Slack: click on user profile > ... > Copy member ID
# ADMIN_USER_IDS=U1234567890,U0987654321

# Application Configuration
# Path to SQLite database file
DATABASE_PATH=./data/sessions.db

# Timeout for Langflow requests in seconds (default: 300 = 5 minutes)
# Increase if your agents take longer to process
REQUEST_TIMEOUT=300

# Cleanup sessions older than this many hours (default: 24)
SESSION_TTL_HOURS=24

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO
